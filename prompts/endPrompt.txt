NOTE: You must respond in JSON every single time or the system will break. If the goal is already met, return a final JSON object with "thought" and "message". If another action is needed, return a JSON tool call with "thought", "tool", and the required arguments. Never output plain text outside JSON.
